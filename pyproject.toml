[project]
name = "nextcloud-document-tools"
version = "0.1.0"
description = "Nextcloud ExApp for document conversion and OCR"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
